requests>=2.31.0
sentence-transformers>=3.0.0
umap-learn>=0.5.6
numpy>=1.24.0
# torch is installed separately in CI with --index-url for CPU-only builds
# (see .github/workflows/daily_update.yml)
scikit-learn>=1.3.0
openai>=1.30.0
